const URL_BASE = 'https://miniback-sprint-2-production.up.railway.app/'

const endpoints = {
    "countries": `${URL_BASE}countries`,
    "flights" : `${URL_BASE}flights/`,
    "ticketPrices": `${URL_BASE}ticketPrices`,
    "avalibleHours": `${URL_BASE}avalibleHours`,
    "reservas": `${URL_BASE}reservas`
}

export default endpoints